<div class="jumbotron">
  <h2>Find the best Hospitals around you with Medapp.</h2>
  <h3>Explore the best of healthcare available in your community.<br>
    Read and leave reviews to assist others seeking the best health care and keep our hospitals in their toes.</h3>
</div>
<h1>Hospitals</h1>

<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hospitals.each do |hospital| %>
      <tr>
        <td><%= hospital.name %></td>
        <td><%= hospital.address %></td>
        <td><%= hospital.phone %></td>
        <td><%= link_to hospital.website, hospital.website %></td>
        <td><%= link_to 'Show', hospital, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_hospital_path(hospital), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', hospital, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Hospital', new_hospital_path, class: "btn btn-link" %>

<%= image_tag "https://s3-us-west-2.amazonaws.com/morafamedapp/hospital+background2.jpg" %>
